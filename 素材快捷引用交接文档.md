# Doodleverse 画布素材快捷引用交接文档

## 一、功能概述

画布节点现在支持不连线直接引用其他节点的素材。

支持的节点：

- AI 文本
- Agent 节点
- 图像生成
- 视频生成
- 语音生成
- 音乐生成

支持的引用方式：

1. 点击节点右上角的“+”按钮。
2. 在节点输入框输入 `@`，自动打开素材选择器。
3. 在选择器中搜索素材名称或素材类型。

选择后会显示 `@素材名` 标签，并在节点右上角显示引用数量。

## 二、素材路由规则

| 目标节点 | 可引用素材 | 下游用途 |
|---|---|---|
| AI 文本 / Agent | 文字、图片、视频 | 文字进入任务上下文，图片和视频进入视觉输入 |
| 图像生成 | 文字、图片 | 文字进入提示词，图片进入参考图 |
| 视频生成 | 文字、图片 | 文字进入提示词，图片进入参考素材 |
| 语音生成 | 文字 | 作为朗读文本 |
| 音乐生成 | 文字 | 作为灵感描述或歌词 |

不支持的素材类型不会出现在对应节点的选择器中。

## 三、保存方式

快捷引用关系保存在节点字段：

```json
{
  "assetReferenceIds": ["text-node-id", "image-node-id"]
}
```

该字段会随以下操作保留：

- 画布自动保存
- 项目重新打开
- QDMY 导入和导出
- 节点复制
- 画布模板复制

如果被引用的素材已经删除，选择器会显示失效引用，并提供“清理失效引用”按钮。

## 四、涉及文件

- `src/components/lovart/CanvasAssetReferencePicker.tsx`：快捷引用选择器。
- `src/components/lovart/CanvasArea.tsx`：画布级入口、`@` 触发和节点挂载。
- `src/lib/canvas-connections.ts`：把快捷引用转换为节点输入。
- `src/lib/qdmy-project.ts`：QDMY 导入导出。
- `src/lib/canvas-shortcuts.ts`：复制节点时重映射素材 ID。
- `src/app/lovart/canvas/CanvasPageClient.tsx`：模板复制时重映射素材 ID。

## 五、验证结果

已通过：

- TypeScript 类型检查。
- ESLint 代码检查。
- 画布连接测试。
- QDMY 项目兼容测试。
- 本地画布页面实际验证。

本地页面验证确认：

- 节点右上角可以看到“快捷引用素材”按钮。
- 点击后能打开搜索选择器。
- 空素材库会显示“没有可引用的画布素材”。
- 页面控制台没有错误。

## 六、上线注意事项

本功能不需要新的环境变量，也不需要新的数据库表。任务日志引用名称需要在现有 `canvas_task_logs` 表增加一个字段。

上线前应确认：

1. 服务器部署的是包含 `assetReferenceIds` 字段的前端和服务端代码。
2. 在 Supabase SQL 编辑器重新执行 `sql/canvas-task-logs.sql`，补齐 `reference_labels` 字段。
3. 现有项目保存接口允许 JSONB 保存数组字段。
4. 已有素材的图片、视频和音频地址仍能通过当前签名访问机制读取。
5. 生产环境完成一次登录后，分别测试 AI 文本、图像生成、视频生成、语音和音乐节点的引用。

## 七、当前限制

- 当前快捷引用是节点级引用，不会自动创建可见连线。
- 音频素材暂不作为 AI 文本、图像或视频模型的输入，因为这些上游接口当前没有统一音频参数。
- 输入 `@` 后会立即打开选择器并移除触发字符；选择素材后不会把完整 URL 暴露给用户。
- 本轮只完成代码、测试和本地页面验证，尚未推送或部署生产服务器。

## 八、建议的下一步

1. 增加跨画布引用失效提示。
2. 在 AI 文本节点中增加引用内容预览，方便发送前确认。

## 九、2026-09-08 续做记录

- 素材选择器新增“已选引用”区域，可直接查看当前节点引用的素材。
- 每个已选素材都可以单独移除，无需回到完整素材列表中查找。
- 新增“一键取消全部”，可以清空当前节点的全部快捷引用。
- 保留原有的失效引用识别和清理操作。
- TypeScript、ESLint、画布连接、节点复制和 QDMY 兼容测试均已通过。
- 任务日志增加“本次引用”标签，显示生成任务实际使用的快捷素材名称，便于定位错误与核对重试输入。
- 引用名称通过 `canvas_task_logs.reference_labels` 同步到云端；上线前需要重新执行 `sql/canvas-task-logs.sql`。
- 已选素材旁新增单项替换按钮，进入替换状态后点击任意候选素材即可原位替换；选择已引用素材时会自动去重。
