# Lovart × 桥豆麻衣酱 V3.4.4 完善路线图

更新日期：2026-07-12

## 1. 目标

在保留 Lovart 在线架构、安全边界和现有产品体验的前提下，迁移桥豆麻衣酱 V3.4.4 中仍有价值的画布、媒体、工作流、素材和生产能力。

本路线图追求功能语义对齐，不追求复刻 Tauri 桌面实现。文件系统、Rust 代理、本地绝对路径和客户端长期密钥等桌面能力，统一改造为浏览器或服务端方案。

## 2. 当前基线

### 已完成

- 图片、文字、形状、路径、视频、图片生成和视频生成节点
- 无限画布、多选、分组、复制粘贴、撤销重做和小地图
- 节点端口、连线、多参考图和结果连线
- 分镜数据、制作板、批量出图和批量视频流程
- 图片对比节点及快照导出
- 局部重绘节点、蒙版保存、羽化和生成
- 图片裁切、去背景、超分、重打光和多角度生成
- QDMY 项目导入、导出、合并和未知字段保留
- 未登录 IndexedDB 保存与登录后项目持久化
- 图片模型隐藏、排序、最近使用和默认参数
- 跨项目生成历史、搜索、分类、收藏和恢复到画布
- Agent 服务端动作、分镜、批量生图、图片编辑和视频执行器
- 大于 100 个节点时的视口裁剪

### 当前主要缺口

- Agent 用户界面已恢复，但真实测试账号下的三类外部动作仍待验收
- 音乐、语音、ComfyUI、视频拆解、动作迁移等原生节点缺失
- 视频模型目录和能力管理不足
- 全局素材库、角色库、姿势参考和对象存储缺失
- 原生 mask inpainting、裁剪重绘和无缝拼回不完整
- 分类项目导入导出、大项目分块处理和云备份缺失
- 节点栏定制、API 管理和生产监控缺失

## 3. 总体原则

1. 先平台后节点：先统一节点注册、任务状态、媒体资产和模型能力，再增加新节点。
2. 服务端持有密钥：API Key、OSS Secret 和第三方凭据不得进入项目 JSON、浏览器存储或画布状态。
3. 节点状态可往返：新增节点必须进入项目保存、IndexedDB、撤销重做和 QDMY 导入导出。
4. 任务必须可恢复：长任务必须有 taskId、状态轮询、失败原因、退款和重试能力。
5. 媒体不直接膨胀项目：新功能优先保存 assetId 和稳定 URL，Data URL 仅作离线降级。
6. 在线优先：不照搬 Tauri 文件路径、Rust 缓存、资源管理器和本地代理。
7. 渐进发布：高风险能力使用服务端或环境 Feature Flag，确保可以快速关闭。

## 4. 目标架构

### 4.1 节点注册中心

建立统一 `NodeDefinition` 注册表，至少包含：

- `type`、名称、图标和分类
- 默认尺寸和默认状态
- 输入/输出端口
- 可连接的数据类型
- 配置面板组件
- 画布渲染组件
- 是否支持运行、取消、重试和批量
- QDMY 类型映射和版本迁移
- Feature Flag 和权限要求

现有 `CanvasElementType`、`getNodePorts`、工具栏菜单及 QDMY switch 逐步改为读取注册表，避免每增加一个节点修改多处 switch。

### 4.2 通用生成任务

统一图片、视频、音频和工作流任务结构：

```ts
type GenerationJob = {
  id: string;
  projectId?: string;
  nodeId: string;
  kind: 'image' | 'video' | 'audio' | 'workflow' | 'analysis';
  status: 'queued' | 'running' | 'succeeded' | 'failed' | 'cancelled';
  progress: number;
  provider: string;
  model: string;
  inputAssetIds: string[];
  outputAssetIds: string[];
  error?: string;
  createdAt: string;
  updatedAt: string;
};
```

前端节点只订阅任务状态，API 路由负责认证、积分、第三方调用、退款和结果入库。

### 4.3 媒体资产层

统一 `ProjectAsset` 与全局媒体资产：

- 支持 image、video、audio、mask、document
- 稳定 assetId、原文件 URL 和缩略图 URL
- 宽高、时长、格式、大小和内容哈希
- 项目归属、用户归属、标签、收藏和来源任务
- 对象存储上传、去重、删除和失败重试
- 离线时允许临时 Data URL，联网后再迁移

### 4.4 模型能力注册表

图片、视频、音乐、语音和分析模型使用统一能力描述：

- provider、modelId、显示名称和类别
- 支持的输入类型、参考图数量和最大文件大小
- 比例、尺寸、时长、音色和高级参数
- 是否异步、是否支持取消和轮询间隔
- 积分成本和服务端环境要求
- 用户隐藏、排序、最近使用和默认配置

## 5. 分阶段实施

| 阶段 | 优先级 | 前置依赖 | 规模 | 核心结果 |
| --- | --- | --- | --- | --- |
| 0 稳定骨架 | P0 | 当前基线 | M | 节点注册、任务类型、Feature Flag |
| 1 Agent 入口 | P0 | 阶段 0 | M | 现有 Agent 能力对用户可用 |
| 2 音乐与语音 | P1 | 阶段 0、任务骨架 | L | 原生音频节点和资产类型 |
| 3 ComfyUI | P1 | 阶段 0、任务骨架 | L | Schema 工作流和多输出 |
| 4 视频升级 | P1 | 模型注册、任务骨架 | L | 多模型、拆解和动作迁移 |
| 5 全局素材库 | P1 | 媒体资产层 | XL | 对象存储和跨项目复用 |
| 6 图像补完 | P2 | 节点注册、媒体资产层 | L | 原生重绘、拼回和工具节点 |
| 7 可移植与性能 | P2 | 媒体资产层 | XL | 分类导出、备份和大项目 |
| 8 高级工具 | P3 | 前述稳定能力 | XL | 3D、剧本、LUT 和管理工具 |

规模用于比较相对复杂度，不代表固定工期；阶段 2、3、4 可在阶段 0 完成后按人员并行，但阶段 5 是阶段 7 的硬依赖。

### 阶段 0：稳定基线与可扩展骨架

优先级：P0

实施状态：进行中。节点注册表已接管端口、创建菜单、默认状态及 QDMY 类型映射；GenerationJob 类型、视频适配、专项测试和最终构建已完成，页面继续拆分仍待完成。

目标：在继续增加节点前，降低 `CanvasPageClient.tsx` 和分散 switch 的维护风险。

交付物：

- 建立节点注册中心第一版
- 把现有图片对比和局部重绘迁移为注册式节点
- 定义通用 GenerationJob 类型和前端状态适配器
- 建立 Feature Flag 工具
- 把 Agent、历史、模型设置等面板状态从超大页面拆出
- 修复现存 `?????` 中文损坏文案
- 为当前未提交阶段 1 功能建立回归测试

验收标准：

- 新增一个演示节点不需要再修改 4 个以上的类型 switch
- 所有现有测试、lint、类型检查和生产构建通过
- 项目保存和 QDMY 往返结果不变
- Feature Flag 关闭时不加载对应节点 UI

### 阶段 1：恢复 Agent 用户入口

优先级：P0

实施状态：界面、模式、取消、防重复提交和错误展示已完成；真实测试账号下的三类外部动作验收仍待完成。

目标：让已经存在的 Agent 服务端和执行器真正可被用户使用。

交付物：

- 恢复可折叠 Agent 面板
- 支持普通、品牌、图片编辑和研究模式
- 显示分析、规划、执行和完成状态
- 展示动作摘要、元数据、后续建议和错误
- 支持选择节点、当前分镜和项目素材上下文
- 增加请求取消、防重复提交和失败重试
- 所有动作继续通过服务端白名单和字段校验

验收标准：

- 用户可从画布打开 Agent 并完成一次分镜创建
- 可完成批量图片生成、编辑选中图片和生成视频
- 非法动作和敏感字段被服务端拒绝
- Agent 失败不会破坏现有画布状态或重复扣费

### 阶段 2：音乐和语音原生节点

优先级：P1

目标：补齐音频媒体类型和两个高价值节点。

交付物：

- `audio` 画布元素和播放器
- 音乐节点：提示词、风格、歌词、时长、纯音乐选项
- 语音节点：文本、音色、语速、情绪和语言
- `/api/generate-music`、`/api/generate-speech` 和状态查询
- 音频结果端口、历史记录、素材库和下载
- QDMY `gen-music`、`gen-speech` 原生往返
- 音频任务积分、退款和使用限额

验收标准：

- 两类节点都能从文字端口接收输入并输出音频
- 刷新页面和重新导入项目后节点及结果可恢复
- 任务失败自动退款并显示明确错误
- 没有配置 provider 时界面给出可操作的配置提示

### 阶段 3：ComfyUI 与可配置工作流

优先级：P1

目标：提供在线安全的 RH App / ComfyUI 工作流节点。

交付物：

- `workflow` 节点类型
- 服务端工作流注册表，不在项目中保存敏感工作流凭据
- 工作流参数 Schema 驱动表单
- 图片、视频、文本和任意结果端口
- 任务提交、进度、取消、重试和结果解析
- RH App 与 RH ComfyUI 两种适配器
- QDMY `comfy-ui` 原生往返

验收标准：

- 管理员可注册一个工作流，用户可从节点填写参数并运行
- 工作流定义更新不会破坏旧项目
- 凭据和完整服务端工作流配置不进入导出文件
- 多输出工作流能生成多个结果资产和端口连线

### 阶段 4：视频能力升级

优先级：P1

目标：把当前 standard/fast 模式升级为完整视频模型和生产工作流。

交付物：

- 视频模型注册表
- Seedance、Sora、Veo、Kling、Wan、Grok Video、Hailuo 的能力描述
- 用户可隐藏、排序、最近使用和设置视频默认参数
- 视频抽帧节点
- 视频拆解/分析节点
- 动作迁移节点
- 音频开关、首尾帧、镜头固定和多素材输入
- 视频任务队列和并发控制

验收标准：

- UI 只展示当前模型真正支持的参数
- 不同模型任务统一进入 GenerationJob 状态流
- 视频分析结果可生成分镜或文本节点
- 动作迁移可连接参考动作和目标图片/视频

### 阶段 5：全局素材、角色与姿势库

优先级：P1

目标：从“当前项目素材栏”升级为可跨项目复用的媒体生产库。

交付物：

- 对象存储适配器和素材数据库
- 图片、视频、音频、蒙版和文档分类
- 搜索、标签、收藏、排序和批量操作
- 跨项目插入和复制
- 角色库及角色参考图
- 姿势参考库；Eagle 集成作为可选扩展，不作为核心依赖
- 素材上传去重、缩略图和删除同步
- 生成历史一键转入素材库

验收标准：

- 大媒体不再直接写入项目 JSON
- 同一文件重复上传不会重复占用对象存储
- 用户只能访问自己的素材或明确共享素材
- 删除素材时能识别项目引用并要求确认

### 阶段 6：图像工作流补完

优先级：P2

目标：补齐恢复版的高级图像节点，同时复用现有 API。

交付物：

- 原生 mask inpainting provider 接口
- 裁剪局部重绘节点
- 无缝拼回节点
- 智能超清节点，复用当前超分 API
- 全局视角节点
- 独立涂鸦画板节点
- 表格编辑节点和 CSV/JSON 导入导出

验收标准：

- 原生 inpainting 能明确区分 mask 与普通参考图
- 裁剪和拼回不改变黑色未选区域
- 超清节点可串入工作流并输出新资产
- 所有新增状态都可保存、撤销和 QDMY 往返

### 阶段 7：项目可移植性、云备份与大项目性能

优先级：P2

目标：让项目从“JSON 可导入”升级为可搬迁、可恢复的生产项目。

交付物：

- 分类项目容器：项目、素材清单、分镜、任务和可选历史
- 导入导出进度、取消和失败报告
- 分批 JSON 解析和资源上传
- 节点增量保存与脏数据追踪
- 图片和缩略图 Worker
- 资源懒加载、内存阈值保护和并发限制
- 项目与生成历史云备份
- 备份清单、去重、恢复、删除同步和重试

验收标准：

- 1000 节点项目可导入，过程中页面保持响应
- 导入可取消且不会产生半个可见项目
- 项目导出后在另一浏览器恢复时媒体引用有效
- 备份失败不影响本地项目继续编辑

### 阶段 8：高级制作工具

优先级：P3

目标：实现低频但差异化明显的专业工具。

交付物：

- 3D 导演台和镜头预设
- LUT 预览、最近使用和色彩应用
- 独立剧本创作工作台
- 剧本拆集、场景和分镜生成
- 可选的指定保存集成；浏览器使用授权目录或下载任务
- 管理员 API/Provider 配置和健康检查

验收标准：

- 3D 导演台输出可进入图片或视频节点
- 剧本项目可转换为现有分镜结构
- 管理端不会向客户端返回任何完整密钥
- 浏览器不支持目录授权时自动降级为下载

## 6. 数据与迁移

### CanvasElement

新增字段必须保持可选，避免旧项目导入失败。对高体积数据只保存 assetId，不直接保存二进制内容。

### QDMY

- 保留 `recoveredDesktop` 作为未知字段兜底
- 新原生节点上线后，将对应降级映射迁移为真实类型
- 导出继续保留桌面类型名和 settings
- 每类新增节点增加独立往返测试

### 数据库

预计新增：

- `generation_jobs`
- `media_assets`
- `asset_tags`
- `asset_favorites`
- `workflow_definitions`
- `provider_configurations`
- `project_backups`

所有表启用行级权限或通过服务端访问；Provider Secret 只保存加密值或环境引用。

## 7. 安全要求

- 所有生成、工作流和存储 API 必须认证
- 第三方 URL 需做协议、域名、超时和大小限制
- 禁止客户端提交任意 ComfyUI 地址或任意服务器动作
- Agent 只允许白名单动作，不接受文件路径、密钥和环境字段
- 上传校验 MIME、扩展名、大小和用户配额
- 下载使用受控 URL，避免任意文件读取
- 日志不得记录完整 Token、Authorization、原始密钥或隐私素材

## 8. 测试策略

每阶段至少包含：

- 纯函数和注册表单元测试
- QDMY 导入导出往返测试
- API 认证、参数和错误测试
- 积分扣费与退款测试
- 画布保存、离线恢复和撤销重做测试
- 至少一条浏览器端完整工作流测试
- lint、TypeScript、专项测试和 production build

关键回归场景：

- 未登录离线项目
- 登录后云端项目
- 旧版 QDMY 降级节点升级
- 多参考图和循环连线拒绝
- 长任务刷新后恢复
- Provider 超时、非 JSON、无结果和重复回调
- 大媒体和 IndexedDB 配额不足

## 9. 发布与回滚

- 每个新节点独立 Feature Flag
- 数据迁移先向后兼容，再开启 UI
- 先内部账号，再白名单用户，最后全量
- 新 Provider 首先只开放测试项目
- 任务失败率、平均耗时、退款率和存储失败率必须可观察
- 关闭 Flag 后旧节点仍可显示、保存和导出，但禁止新建或运行

## 10. 第一批实施清单

第一批只做阶段 0 和阶段 1，避免同时引入新媒体类型。

- [x] 建立 NodeDefinition 注册表及测试
- [x] 迁移图片对比、局部重绘端口和菜单定义
- [x] 建立 Feature Flag 工具
- [x] 从 `CanvasPageClient.tsx` 提取 Agent 面板状态和提交控制器
- [x] 恢复 Agent 面板入口
- [x] 增加取消、防重复提交和错误恢复
- [x] 修复当前 `?????` 损坏文案
- [x] 跑 lint、类型检查、专项测试、浏览器冒烟测试和生产构建

实施记录（2026-07-12）：第一批工程项已完成。Agent 面板的真实外部模型调用仍需在配置有效凭据的测试账号下做三类动作验收，完成后才能将阶段 1 整体标记为完成。

第一批完成门槛：

- 不改变现有项目格式
- Agent 可由用户实际打开并完成至少三类动作
- 现有图片、视频、分镜、对比和重绘流程无回归
- 所有检查为绿色后再进入音乐与语音节点开发

## 11. 明确不做

- 不恢复客户端长期保存第三方 Secret
- 不把 Tauri 任意文件读写搬到浏览器
- 不允许任意本地命令或任意 ComfyUI 地址执行
- 不恢复已经移除的支付宝充值流程
- 不为了形式对齐复制桌面端的 Rust 缓存和代理
- 不在对象存储完成前继续扩大 Data URL 项目负担
