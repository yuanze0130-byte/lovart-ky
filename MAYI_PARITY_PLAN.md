# 麻衣画布功能对齐计划

依据恢复包 `bundle-feature-index.json`、节点目录和本地桥接能力，对 Lovart 当前实现逐项核对。

## 当前覆盖

| 功能域 | 状态 | 当前情况 |
| --- | --- | --- |
| 图片、文字、图片生成、视频生成 | 已实现 | 已有原生节点、端口连线和生成面板 |
| 分镜 | 已实现 | 已有分镜资产、布局和图像/视频生成流程 |
| 多选、分组、复制粘贴、撤销重做 | 已实现 | 画布历史最多保留 100 个快照 |
| 多参考图连线 | 已实现 | 提示词、参考图、首帧和尾帧端口已区分 |
| 项目导入导出 | 已实现 | QDMY 节点、连线、分组、视图和模型设置可往返 |
| 离线保存 | 已实现 | 未登录时保存到 IndexedDB |
| 批量生图 | 已实现 | 1/2/4/8 张，支持异步并行和同步顺序 |
| 局部重绘 | 部分实现 | 已有对象检测、区域选择和编辑，但缺少独立蒙版节点、羽化及无缝拼回流程 |
| 图片对比 | 未原生实现 | 导入时当前降级为普通图片，缺少双图滑杆和对比导出 |
| 历史资源库 | 部分实现 | 有撤销历史和项目资产，缺少跨项目生成历史、筛选、收藏和恢复 |
| 模型管理 | 部分实现 | 有 11 个模型注册表，缺少模型隐藏/排序、最近使用和全局默认参数 |
| 音乐、语音 | 未实现 | 导入时降级为文字节点 |
| Comfy UI 工作流 | 未实现 | 导入时降级为图片生成节点，仅保留原始恢复字段 |
| 自定义节点栏 | 未实现 | 缺少常用节点选择、排序和持久化 |
| 本地桥接 | 未实现 | 缺少 health/capabilities/state/action 接口和受限动作执行器 |
| OSS/云端历史备份 | 未实现 | 在线版应由服务端托管密钥，不能照搬桌面端浏览器存储方案 |

## 实现顺序

### 阶段 1：高频图像工作流

1. 原生 `image-compare` 节点：双输入端口、滑杆对比、交换图片、导出快照。
2. 原生 `inpaint-menu` 节点：原图和蒙版输入、画笔/擦除/羽化、裁剪重绘、无缝拼回。
3. 生成历史抽屉：记录模型、提示词、参考图、任务状态和生成结果，可一键放回画布。
4. 模型管理：隐藏、排序、最近使用、每模型默认比例/分辨率/数量/执行方式。

验收标准：上述状态进入项目保存、离线保存、撤销重做和 QDMY 往返，不产生只存在于面板内的临时配置。

### 阶段 2：媒体与生产能力

1. 原生音乐节点：文本到音乐、时长、风格、歌词和音频结果端口。
2. 原生语音节点：文本、音色、语速、情绪和音频结果端口。
3. 视频生成增强：多模型注册、音频开关、镜头固定、素材列表和任务队列。
4. 素材库增强：图片/视频/音频分类、标签、搜索、收藏和跨项目复用。

### 阶段 3：工作流与自动化

1. Comfy 工作流节点：服务端工作流配置、参数表单、任务提交和结果端口。
2. 常用节点自定义：节点栏显示、顺序和最近使用持久化。
3. 批量动作协议：创建节点、更新节点、多参考连接、批量运行和创建图片系列。
4. 安全本地桥接：只暴露白名单动作，不返回密钥、隐藏设置或完整提示词库。

### 阶段 4：存储与可靠性

1. 服务端对象存储适配器：S3/OSS/七牛/Cloudinary 统一上传接口。
2. 项目与历史资源备份：清单、去重、冷恢复、删除同步和失败重试。
3. 大项目性能：连线分层、节点增量保存、生成队列并发限制和资源懒加载。

## 保留的产品决策

- 不恢复已移除的旧提示词模板库、反推提示词和支付宝充值流程。
- Agent 自动化后续采用受限动作协议，不恢复旧的右侧提示词聊天栏。
- 桌面端长期密钥和本地文件路径能力需要改造成在线服务端方案，不直接复制到浏览器。
