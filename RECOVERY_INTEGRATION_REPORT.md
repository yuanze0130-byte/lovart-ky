# 桥豆麻衣酱恢复成果集成报告

## 已完成的在线画布优化

### 桌面项目兼容

- 新增 `src/lib/qdmy-project.ts`，支持桌面 `nodes + connections + groups + view` 与在线 `CanvasElement[]` 双向转换。
- 保留提示词、模型、平台、比例、分辨率、引用关系、连线与分组。
- 不认识或暂未原生显示的字段保存在 `recoveredDesktop`，再次导出不会静默丢失。
- Agent、Comfy UI、音乐、语音、分镜、对比和局部重绘节点可降级显示，并恢复原类型导出。
- 画布标题栏新增导入、导出命令及操作结果提示。
- 导入先显示节点、连线、分组和警告预览，可选择替换或合并，不再直接覆盖画布。
- 合并自动重映射重复 ID、分组、引用图、关联节点和连线端点。

### 离线可靠性

- 新增 `src/lib/local-canvas-store.ts`。
- 未登录时使用 IndexedDB 自动保存标题和画布元素，不再刷新即丢失。
- 浏览器真实验证已创建 `lovart-local-canvas` 数据库并显示“已保存到本机”。

### 大画布性能

- 100 个以上节点时启用带缓冲区的视口裁剪。
- 选中节点和同组选中节点不会被裁掉。
- 连线端点查找由每条连线遍历所有节点改为 Map 索引。
- 仅渲染与可见节点相关的连线，小项目保持原行为。

## 自动验证

- `npm run test:qdmy`：6 类代表节点、2 条连线和 1 个分组通过导入/导出往返。
- TypeScript：`tsc --noEmit` 通过。
- ESLint：通过。
- `next build`：34 个路由生产构建通过。
- Edge/CDP：导入、导出、本地保存状态和 IndexedDB 均验证，无控制台异常。
- Edge/CDP 真实注入 QDMY 文件：预览统计及“合并/替换”操作均正常显示。

## 已知问题

- Next 16 开发模式的 Turbopack 在当前 Windows 环境发生一次 `globals.css` panic；生产构建和生产服务器正常。
- 项目中原有多处中文乱码，部分已经丢失字节，不能安全批量转码，应结合恢复包原文逐项修复。
- OSS、七牛、Cloudinary 应改造成在线服务端对象存储适配器，避免在浏览器暴露长期密钥。
- `CanvasPageClient.tsx` 体积过大，后续应按项目传输、Agent、分镜、迷你地图和生成流程拆分。
