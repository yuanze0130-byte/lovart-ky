# Doodleverse 借鉴记录：桥豆麻衣酱 V3.5.0

记录日期：2026-09-01
分析对象：`桥豆麻衣酱_V.3.5.0.exe`

## 分析说明

- 文件版本：3.5.0
- 文件大小：75,876,352 bytes
- SHA-256：`899301E3647E1B580DDF885F5EA1CE1F54F441CEBCA5D6020069F7AE52F6A165`
- 封装：Tauri 2.9.5 / WebView2 / Wry，64 位 Windows PE
- 分析方式：只读静态提取，未运行 EXE
- 已提取前端资源：`index-DtcW1Jmy.js`（8,188,979 bytes）和 `index-IgfTMlkn.css`（602,984 bytes）
- 限制：生产包没有 source map，可以恢复业务逻辑、模型映射、配置和 UI 文案，但不能恢复原始文件结构、变量名、注释和 TypeScript 类型。

## 已确认的模型映射

| UI ID | UI 名称 | 实际上游 modelName |
|---|---|---|
| `deepseek-v4-exp` | DeepSeek V4 Exp | `deepseek-v4-flash-vision-exp` |
| `glm-5.3` | GLM 5.3 | `glm-5.3` |
| `qwen3.8-max` | Qwen 3.8 Max | `qwen3.8-max` |
| `wan-3.0` | Wan 3.0 | `wan3.0-video` |
| `wan-3.0-prime` | Wan 3.0 Prime | `wan3.0-video-prime` |
| `seedance-2.5-sdols` | Seedance 2.5 API | `doubao-seedance-2.5` |
| `seedance-2.5-huya-volc` | Seedance 2.5 虎牙/火山 | `doubao-seedance-2-5-260628` |

Wan 3.0 还定义了关键帧模式 `wan-3.0-key-frames`，支持 Auto、16:9、9:16、1:1、4:3、3:4，分辨率为 480P/720P/1080P，时长为自适应或 2–30 秒。

Seedance 2.5 存在普通版、低价版及多个渠道分支。接入服务器版时不能只增加显示名称，必须同步处理实际模型 ID、请求参数、能力限制、状态查询和价格。

## Codex 生图

- 前端调用 Tauri 命令 `run_codex_image_generation`。
- 本地工作器路径为 `electron/codex-image-worker.mjs` 或 `resources/electron/codex-image-worker.mjs`。
- 请求包含 prompt、ratio、background、references、nodeId、taskId。
- 记录 Codex task/thread、最终回复和 8%/18%/92%/100% 等任务阶段，超时约 10 分钟。
- 设置页会检测 Codex 账号登录状态和生图能力，并要求先在 Codex 登录。
- 本地画布桥接协议支持 `/health`、`/capabilities?mode=quick`、创建节点、连接参考、批量生图和运行生成节点。

该方案依赖桌面端本地进程、本机 Codex 登录状态和本地 worker，不能原样复制到 Web 服务器。Doodleverse 如需接入，应实现独立的服务端任务适配器和受支持的授权流程。

## 视频剪辑时间线

- 独立节点类型：`video-timeline`。
- 支持添加视频、设置 sourceStart/sourceEnd、片段排序、预览和全屏。
- 合并导出调用 `export_video_timeline`，输出 MP4。
- Rust 端寻找 `C:\ffmpeg\ffmpeg.exe`、`C:\ffmpeg\bin\ffmpeg.exe`、Program Files 或 PATH 中的 FFmpeg。
- 支持远程视频下载至临时目录，导出结束后清理临时文件。

Doodleverse 可复用“时间线 JSON + FFmpeg 导出”的数据结构，但服务器应使用 Linux FFmpeg 后台任务队列，不能复制 Windows 本地路径。

## 触屏交互

- 核心画布采用 Pointer Events。
- 使用 `setPointerCapture`、`releasePointerCapture` 和 `touchAction=none`。
- 维护多个触点，支持双指手势。
- 已实现单指拖动画布、单指拖动节点、双指捏合缩放。

## 连线与性能

- 支持连线全显/半显切换。
- 支持调整连线粗细和透明度。
- 生成动画、流光动画、导航可以独立关闭。
- Doodleverse 已有视口裁剪和低精度显示基础，可在现有结构上补充半显逻辑。

## 日志与 Agent 上下文

- 独立日志面板支持全部、日志、警告、错误筛选。
- 支持清空日志和拖动调整面板高度。
- Agent 生成“会话上下文日志（AI 启动必读）”。
- 上下文包含上轮日志、最近成品和用户已经确认的选择。
- 上下文有长度限制，避免长时间运行后无限增长。
- Agent 工具协议要求形成创建节点、连接参考、写入提示词、发送任务的完整链路。
- Agent 能检测未完成任务并继续补齐缺少的生成结果。

## 标记与素材快捷引用

- 图片标记生成文字说明和蒙版。
- 标记说明及蒙版会进入生成提示词和请求。
- 标记栏可以作为生成节点的上游输入。
- 节点底部有参考素材快捷栏，可从画布选择参考、调整顺序和取消引用。
- Chat 支持 `@` 引用素材，角色素材支持使用 `@名称` 引用。

## 3D 导演台

- 导演视角、机位视角和俯视视角。
- WASD 移动、Shift 奔跑、Ctrl 下蹲、空格跳跃。
- 动画库和动画时间轴。
- 自动 K、手动关键帧、上一/下一关键帧、复制/粘贴关键帧。
- 父级和链接；子级继承父级的移动、旋转和缩放。

## Doodleverse 实施顺序

1. 完善模型目录、能力描述和上游映射，不直接复制桌面版价格。
2. 将画布交互统一为 Pointer Events，补齐手机和触屏支持。
3. 增加统一任务日志面板，支持复制错误和重新发送。
4. 持久化 Agent 对话摘要、最近成品、确认决策和执行计划。
5. 增加不要求可见连线的素材引用槽位与 `@` 资产选择。
6. 将标记说明、区域和蒙版定义为标准下游输入。
7. 增加连线全显/半显模式。
8. 使用服务器 FFmpeg 队列实现视频时间线合并导出。
9. 扩展 3D 导演台动画系统、角色控制和父子层级。

## 不直接复制的内容

- EXE 中的第三方服务地址、密钥字段和渠道绕过逻辑。
- Codex 本地登录状态、桌面 worker 和本地桥接令牌。
- Windows 绝对路径以及 `C:\ffmpeg` 专用逻辑。
- 桌面端本地缓存、OSS 和 SQLite 混合实现。
- 压缩后的生产代码。

所有借鉴功能应根据行为重新实现到 Doodleverse 当前架构中，并分别完成权限、扣费、任务状态、失败恢复和服务器资源限制验证。

## 2026-09-07 线上画布继续优化记录

### 已完成：AI 节点基础闭环

- 新增 AI 文本、独立 Agent 和 AI 表格节点。
- AI 文本支持上游文字、表格、图片和视频关键帧输入。
- AI 表格支持结构化 JSON 校验、Markdown 展示和白底表格图片输出。
- Agent 节点支持独立角色设定、任务要求、模型选择和文字结果输出。
- 新增服务端模型白名单、价格确认、认证、限流、并发预算、失败退款和错误脱敏。
- QDMY 的 `custom-agent` 映射为独立 Agent，节点字段可保存和恢复。

### 已完成：语音与音频素材基础闭环

- 新增「语音生成」画布节点，可连接上游文字或直接输入文本。
- 支持语音模型、音色、语速、音调以及 MP3/WAV/FLAC 格式。
- 新增 `/api/generate-speech` 服务端接口，密钥只在服务端使用，调用 MiniMax T2A 协议。
- 语音生成执行积分扣费、限流、模型白名单和价格变更保护。
- 音频素材支持上传、远程导入、签名访问、范围请求和云端持久化。
- 新增音频输出端口，可连接到音频节点或后续处理节点。
- QDMY 的 `gen-speech` 可导入为语音生成节点。

### 配置前提

2026-09-08 用户确认：语音与之前生图共用 Comfly 密钥，无需另行申请 MiniMax 密钥。现有语音代码支持回退读取 `XAI_API_KEY`；实际生图使用哪个环境变量及语音路径仍需在接入时核对。未进行真实语音上游调用。

### 2026-09-08 Comfly 语音价格核对

来源：https://ai.comfly.org/en/models?provider=Minimax 。已通过浏览器读取页面，以下保留页面显示单位 ฿，不直接等同于人民币或站内积分。

| 官方格式模型（目录 ID） | default 分组页面价格 |
|---|---|
| minimax/speech-01-hd | ฿3.5 / 万字符 |
| minimax/speech-01-turbo | ฿2 / 万字符 |
| minimax/speech-02-hd | ฿3.5 / 万字符 |
| minimax/speech-02-turbo | ฿2 / 万字符 |
| minimax/speech-2.6-hd | ฿3.5 / 万字符 |
| minimax/speech-2.6-turbo | ฿3.5 / 万字符 |

- 页面另列无 `minimax/` 前缀的 OpenAI TTS 格式模型，显示按百万 token 的价格，不能混用两种接口和报价。
- 当前页面展示的 37 个 MiniMax 模型中没有 speech-2.8-hd / speech-2.8-turbo。此前代码默认的 2.8 模型及每次 3/2 积分不是已核实的 Comfly 报价。
- 2026-09-08 已完成运行代码调整：目录 ID 使用 `minimax/speech-*`，发送 MiniMax 原生请求时映射为无前缀的 `speech-*`；移除未上架的 2.8 默认模型。
- 语音与生图共用 `XAI_API_KEY` / `XAI_BASE_URL`，接口路径为 `${XAI_BASE_URL}/minimax/v1/t2a_v2`。
- 计费改为按 Unicode 字符动态报价，沿用 1 Comfly 成本单位 = 15 积分、5% 加价、向上取整。页面 ฿2/万字符对应 32 积分/万字符，฿3.5/万字符对应 56 积分/万字符；少量文本最低 1 积分。
- 前端展示字符数、页面单价与本次积分，服务端重新计算并核对报价后扣费。已增加定价边界和模型映射测试；仍未部署。
- 2026-09-08 继续补齐桌面端语音闭环：帮助页新增六个语音模型的上游单价和万字符积分表；语音任务写入画布日志，记录排队、生成、成功、素材保存警告和失败状态。
- 模型目录接口会检查服务器是否已经配置共用的 `XAI_API_KEY`；未配置时节点关闭生成按钮并显示原因。生成成功会刷新账户积分余额。
- 语音失败任务现可从画布日志直接重新运行；日志同步保留模型、节点、错误和上游请求编号。语音定价、任务日志、日志安全、TypeScript、代码检查和生产构建均已通过。

### 当前状态

- TypeScript、AI、连接、QDMY、画布工具和生产构建检查均通过。
- 音乐节点基础闭环已完成本地实现：`gen-music` 现在映射为独立音乐生成节点，不再退化为普通文字节点。
- 本轮只完成本地代码和文档更新，未推送、未部署生产服务器。

### 2026-09-08 Suno 音乐节点

- 已核对 Comfly Suno 目录：`suno_music` 的 default 分组价格为 ฿0.5/次。沿用 1 上游成本单位 = 15 积分、5% 加价并向上取整，站内固定报价为 8 积分/次。
- 新增灵感模式、自定义歌词模式和纯音乐开关；版本支持 v5.5 `chirp-fenix`、v5 `chirp-crow`、v4.5+ `chirp-bluejay` 与 v4 `chirp-v4`。
- 提交接口使用 `/suno/submit/music`，状态接口使用 `/suno/fetch/{task_id}`；服务端持有共用 `XAI_API_KEY`，浏览器无法读取上游密钥。
- 一次任务通常返回两首候选，节点会同时展示并把两首音频导入画布云端素材；首个结果作为节点的音频输出。
- 新增服务端模型/版本白名单、输入长度限制、价格变更校验、用户限流、异步任务所有权校验、失败结算与自动退款。
- 画布任务日志记录排队、生成进度、成功、素材保存警告和失败；支持从日志重新运行节点。
- 已增加音乐价格测试，并补充 QDMY 导入导出字段和节点连接测试。尚未发起真实付费音乐任务，尚未部署。

### 2026-09-08 素材快捷引用

- AI 文本、Agent、图片生成、视频生成、语音和音乐节点新增统一“+”素材入口；无需创建可见连线即可引用画布上的文字与媒体。
- 在上述节点的输入框键入 `@` 会移除触发字符并直接打开素材选择器，支持按名称或素材类型搜索。
- 已选择素材以 `@素材名` 标签显示；选择器提供图片缩略图、文字/视频/音频类型图标、选中状态与失效引用清理。
- 快捷引用保存为节点的 `assetReferenceIds`，随项目持久化，并加入 QDMY 导入导出、节点复制与模板复制的 ID 重映射。
- 发送任务时会按目标节点能力自动路由：文字进入提示词，图片进入参考图，视频进入 AI 视频输入；不支持的素材类型不会显示在该节点的选择器中。
- 任务日志现会记录并展示本次快捷引用的素材名称，图片、视频、语音和音乐任务均可直接看到 `@素材名`；名称随云端任务日志同步保存。
- `canvas_task_logs` 新增 `reference_labels` 字段，部署前需重新执行 `sql/canvas-task-logs.sql` 完成兼容迁移。
- 快捷引用选择器新增单项“替换”操作：点击已选素材旁的替换按钮，再选择新素材即可原位替换；重复素材会自动去重，引用顺序保持稳定。
